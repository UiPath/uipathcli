[project]
name = "research-agent"
version = "0.0.1"
description = "A research agent that searches the web and provides structured answers"
authors = [{ name = "Developer", email = "developer@example.com" }]
dependencies = ["uipath>=2.1.87"]
requires-python = ">=3.10"
